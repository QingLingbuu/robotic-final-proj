# RoboCasa + RL dependency baseline for migration work.
# IMPORTANT: treat this as a separate environment from the robosuite/mink baseline.
# Real RoboCasa install resolved numpy 2.2.5 / torch 2.7.1 / torchvision 0.22.1 / gymnasium 0.29.1.
# mink 0.0.5 requires numpy<2.0.0, so do not merge this baseline into the mink-enabled env.

numpy==2.2.5
numba==0.61.2
scipy==1.15.3
mujoco==3.3.1
pygame
Pillow==10.2.0
opencv-python==4.8.0.76
pyyaml==6.0.1
pynput
tqdm==4.66.2
termcolor
imageio
h5py
lxml
hidapi

torch==2.7.1
torchvision==0.22.1
gymnasium==0.29.1
stable-baselines3

transformers==4.41.2
huggingface-hub

# Install RoboCasa after the separate RoboCasa/RL env is created.
# The recommended path is editable install from third_party/:
#   mkdir third_party
#   cd third_party
#   git clone https://github.com/ARISE-Initiative/robosuite
#   git clone https://github.com/robocasa/robocasa
#   cd ..
#   pip install -e ./third_party/robosuite
#   pip install -e ./third_party/robocasa
